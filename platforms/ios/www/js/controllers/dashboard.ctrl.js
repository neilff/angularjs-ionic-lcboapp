/**
 *  This controller manages the dashboard / navigation.
 *
 *  @module DashboardController
 */
angular.module('lcboApp.info', [])
    .controller('DashboardController', ['$scope', '_', function($scope, _) {
        'use strict';

        /**
         *  Define $scope methods
         */
        _.extend($scope, {

        });
    }]);