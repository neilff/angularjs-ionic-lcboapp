/**
 *  This controller manages the about pages.
 *
 *  @module AboutController
 */
angular.module('lcboApp.info', [])
    .controller('AboutController', ['$scope', '_', function($scope, _) {
        'use strict';

        /**
         *  Define $scope methods
         */
        _.extend($scope, {

        });
    }]);