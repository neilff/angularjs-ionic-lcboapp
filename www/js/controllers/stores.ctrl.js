/**
 *  This controller manages all stores information
 *
 *  @module StoresController
 */
angular.module('lcboApp.stores', [])
    .controller('StoresController', ['$scope', '_', function($scope, _) {
        'use strict';

        /**
         *  Define $scope methods
         */
        _.extend($scope, {

        });
    }]);